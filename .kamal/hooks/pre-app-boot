#!/bin/sh
set -e

# Database migrations are embedded in the Rust binary and applied automatically
# when the application boots (see back-end/src/main.rs `sqlx::migrate!`), so no
# explicit migration step is needed here.
echo "[$KAMAL_VERSION] Migrations run automatically on application boot."
